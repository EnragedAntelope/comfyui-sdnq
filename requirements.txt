# ComfyUI-SDNQ Requirements
# SDNQ quantization engine
sdnq @ git+https://github.com/Disty0/sdnq.git

# Diffusers and transformers
# Recommended: diffusers>=0.35.2 for FLUX.2, video, and multimodal models
# Minimum: diffusers>=0.20.0 (image models only, no video/multimodal support)
# Note: AutoPipeline (for video/multimodal) requires diffusers>=0.30.0
diffusers>=0.20.0
transformers>=4.40.0

# HuggingFace Hub integration
huggingface-hub>=0.20.0

# Model serialization
safetensors>=0.4.0

# PyTorch (required but usually pre-installed with ComfyUI)
torch>=2.0.0

# Acceleration utilities
accelerate>=0.25.0
